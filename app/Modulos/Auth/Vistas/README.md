# Vistas — módulo Auth

| Archivo | Uso |
|---------|-----|
| `index.php` | Portal FVD: formulario de login y shell (también servido desde `index.php` en la raíz) |

Los endpoints JSON de sesión están en `Controladores/` (`login`, `logout`, `auth_context`, `mi_perfil`).
